/*PROD*/
Prod.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('prodAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('prodAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('prodCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('prodContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('prodData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('prodGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('prodMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('prodPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('prodGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('prodRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('prodSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('prodTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('prodTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('prodVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('prodTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*CAC*/
Cac.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('cacAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('cacAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('cacCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('cacContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('cacData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('cacGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('cacMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('cacPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('cacGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('cacRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('cacSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('cacTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('cacTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('cacVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('cacTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*XnetAni*/
XnetAni.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('XnetAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('XnetAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('XnetCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('XnetContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('XnetData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('XnetGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('XnetMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('XnetPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('XnetGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('XnetRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('XnetSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('XnetTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('XnetTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('XnetVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('XnetTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*RIT*/
Rit.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('ritAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('ritAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('ritCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('ritContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('ritData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('ritGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('ritMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('ritPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('ritGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('ritRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('ritSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('ritTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('ritTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('ritVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('ritTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*RIM*/
Rim.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('rimAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('rimAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('rimCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('rimContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('rimData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('rimGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('rimMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('rimPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('rimGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('rimRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('rimSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('rimTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('rimTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('rimVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('rimTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*RENOUV*/
Renouv.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('renouvAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('renouvAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('renouvCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('renouvContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('renouvData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('renouvGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('renouvMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('renouvPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('renouvGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('renouvRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('renouvSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('renouvTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('renouvTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('renouvVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('renouvTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*BENCH*/
Bench.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('benchAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('benchAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('benchCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('benchContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('benchData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('benchGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('benchMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('benchPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('benchGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('benchRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('benchSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('benchTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('benchTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('benchVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('benchTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
/*Formation*/
Formation.forEach(function (aff) {
    if(aff.Application === "ADDAGIO"){
        document.getElementById('FormationAdda').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "AVISE") {
        document.getElementById('FormationAvise').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CAMELEON"){
        document.getElementById('FormationCam').innerHTML = aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "CONTRALTO"){
        document.getElementById('FormationContr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "DATAHUB"){
        document.getElementById('FormationData').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRC SIEBEL"){
        document.getElementById('FormationGrc').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "MAESTRO"){
        document.getElementById('FormationMaestro').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application ==="PRESTO"){
        document.getElementById('FormationPresto').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "GRAF"){
        document.getElementById('FormationGraf').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "RONDO"){
        document.getElementById('FormationRondo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "SOPRANO"){
        document.getElementById('FormationSopr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TEMPO"){
        document.getElementById('FormationTempo').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TRIBOX"){
        document.getElementById('FormationTribox').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "VIBRALTO"){
        document.getElementById('FormationVibr').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
    else if (aff.Application === "TABGAR"){
        document.getElementById('FormationTabgar').innerHTML += aff["Packages de livraison"][0] + '<br/>'
    }
});
